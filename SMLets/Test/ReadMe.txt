This is where the test code should be located.
To run the tests, add the test to the Invoke-Test.ps1 script. The
if the test passes, the script should exit 0
if the test fails, the script should exit non-0
